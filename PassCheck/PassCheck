using System;
using System.Linq;



    public class Program
    {
        private static string lower="abcdefghijklmnopqrstuvwxyz";
        private static string upper="ABCDEFGHIJKLMNOPQRSTUVWXYZ";
        private static string numbers="0123456789";
        private static string special1="=_.+-<>`*/|[]{}";
        private static string special2="~!@#$%^&\\*()?:;'";


    private  static void CheckPAssword(string password )
     { 
    Console.WriteLine("Please enter your password ");
    string password=Console.ReadLine();

    int score = 0;
    if (password <8) Console.WriteLine("Your password is too short");
    if (password >=8) score++;
    if (password>=12) score++;
    if (password>=16) score++;
    

    if (password.Any(char.IsLower)) score++;
    if (password.Any(char.IsUpper)) score++;
    if (password.Any(char.IsDigit)) score++;
    if (password.Any(c1=>special1.Contains(c1)) )score++;
    if (password.Any(c2=>special2.Contains(c2)) )score++;

        switch(score)
        {
          //case score==2:
          case 2:
          Console.ForegroundColor=ConsoleColor.DarkRed;
          Console.WriteLine("Youre password has minimal requeirements");
          break();
         
          case 3:
          case 4:
          Console.ForegroundColor=ConsoleColor.DarkYellow;
          Console.WriteLine("Youre password has average strongness");
          break;

          case 5:
          Console.ForegroundColor= ConsoleColor.Green;
          Console.WriteLine("Youre password is  strong ");
          break;

          case 6:
          Console.ForegroundColor=ConsoleColor.DarkGreen;
          Console.WriteLine("Youre password is very strong ");
          break;

        }
       Console.ResetColor();
     }
       
  private  static string Generate()
    {
        
     Console.WriteLine("Write lenght for your password \n");
     int length=Convert.ToInt16 (Console.ReadLine());

     if (length<8) length=8;
     if (length >20) length= 20;

    //miavorel bolor tipi simvolner@
    string Symbols=upper+lower+special1+special2+numbers;
    char[] NewPassword=new char[length]  ;

    Random rand= new Random();
    for (int i=0;i<length;i++)
        {
            NewPassword[i]= Symbols[rand.Next(Symbols.Length)];

        }

  return new string (NewPassword);
   
  }

        static void Main(string[] args)
        {
    CheckPassword();
   // Console.WriteLine("Youre passwod has {score}");

    string mypassword1=Generate();
    Console.WriteLine($"\n{mypassword1}");

      Console.ReadKey();
        }
    }

